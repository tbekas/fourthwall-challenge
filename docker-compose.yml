version: '3.3'

services:
  app:
    build: ./
    environment:
      SPRING_DATA_MONGODB_HOST: mongo
      SPRING_REDIS_HOST: redis
    depends_on:
      - mongo
      - redis
  mongo:
    image: mongo:4.2
    restart: always
  redis:
    image: redis:6.0
    restart: always
